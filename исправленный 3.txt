""" HybridCache FULLSPEED SUITE v2.3 — ФСТЭК-совместимая версия - Поддержка КриптоПро CSP 5.0+ - Управление ключами через контейнеры - Логирование операций безопасности - Соответствие ФСТЭК №21, №38, №26 - Исправления: AES удалён, кросс-платформенная поддержка, проверка версии CSP """ import os import sys import time import hashlib import json import logging import secrets import subprocess from typing import List, Optional from collections import defaultdict from pathlib import Path if os.name == 'nt': import win32com.client # === Настройка логирования безопасности === SECURITY_LOG = os.getenv("HYBRIDCACHE_SECURITY_LOG", "hybridcache_security.log") logging.basicConfig( filename=SECURITY_LOG, level=logging.INFO, format="%(asctime)s [SECURITY] %(message)s", encoding="utf-8" ) # === Глобальные настройки === CACHE_DIR = os.getenv("HYBRIDCACHE_DIR", "C:/HybridCache") SOURCE_DIR = os.getenv("HYBRIDCACHE_SOURCE", "//Server/OfficeDocs") MAX_CACHE_SIZE = int(os.getenv("HYBRIDCACHE_MAXSIZE", 100 * 1024**3)) CACHE_TTL = int(os.getenv("HYBRIDCACHE_TTL", 60 * 64 * 24 * 7)) ENABLE_ENCRYPTION = os.getenv("HYBRIDCACHE_ENCRYPTION", "1") == "1" ENCRYPTION_MODE = "gost" # Только ГОСТ для ФСТЭК # === КриптоПро: имя контейнера === GOST_CONTAINER = os.getenv("HYBRIDCACHE_GOST_CONTAINER", "HybridCache_GOST") ACCESS_COUNT_FILE = os.path.join(CACHE_DIR, ".access_count.json") access_counts = defaultdict(int) def load_access_counts(): global access_counts if os.path.exists(ACCESS_COUNT_FILE): try: with open(ACCESS_COUNT_FILE, "r", encoding="utf-8") as f: access_counts.update(json.load(f)) except Exception: pass def save_access_counts(): try: with open(ACCESS_COUNT_FILE, "w", encoding="utf-8") as f: json.dump(dict(access_counts), f) except Exception: pass def integrity_path(cpath: str) -> str: return cpath + ".sha256" def ensure_cache_dir(): os.makedirs(CACHE_DIR, exist_ok=True) def cache_path(filename: str) -> str: safe_name = filename.replace("/", "_").replace("\\", "_").replace("..", "") return os.path.join(CACHE_DIR, safe_name) def is_expired(filepath: str) -> bool: try: return (time.time() - os.path.getmtime(filepath)) > CACHE_TTL except Exception: return True def get_total_cache_size() -> int: total = 0 for f in os.listdir(CACHE_DIR): if f.endswith(".sha256") or f == ".access_count.json": continue try: total += os.path.getsize(cache_path(f)) except Exception: pass return total def enforce_cache_limit(): load_access_counts() files = [] for f in os.listdir(CACHE_DIR): if f.endswith(".sha256") or f == ".access_count.json": continue cp = cache_path(f) try: mtime = os.path.getmtime(cp) size = os.path.getsize(cp) score = mtime + access_counts.get(f, 0) * 86400 files.append((f, score, size)) except Exception: pass total = sum(size for _, _, size in files) if total <= MAX_CACHE_SIZE: return files.sort(key=lambda x: x[1]) for f, _, size in files: if total <= MAX_CACHE_SIZE: break try: os.remove(cache_path(f)) ipath = integrity_path(cache_path(f)) if os.path.exists(ipath): os.remove(ipath) access_counts.pop(f, None) total -= size except Exception: pass save_access_counts() # ==== ИНТЕГРАЦИЯ С КРИПТОПРО CSP ==== if os.name == 'nt': def _init_csp() -> win32com.client.Dispatch: try: csp = win32com.client.Dispatch("CPCSP.CSP") version = csp.GetVersion() if version < "5.0": raise RuntimeError("Требуется КриптоПро CSP 5.0+") logging.info(f"Инициализация КриптоПро CSP успешна, версия: {version}") return csp except Exception as e: logging.error(f"Ошибка инициализации КриптоПро CSP: {e}") raise RuntimeError("Не удалось инициализировать КриптоПро CSP") def _encrypt_gost_csp(data: bytes, container_name: str) -> bytes: try: csp = _init_csp() if not csp.IsContainerExist(container_name): logging.error(f"Контейнер {container_name} не найден") raise RuntimeError(f"Контейнер {container_name} не существует") iv = secrets.token_bytes(16) encrypted = csp.EncryptData(container_name, data, iv, "GOST3412_2015_CTR") logging.info(f"Шифрование через КриптоПро: контейнер={container_name}, размер={len(data)}") return iv + encrypted except Exception as e: logging.error(f"Ошибка шифрования ГОСТ: {e}") raise RuntimeError(f"Не удалось зашифровать данные через КриптоПро CSP: {e}") def _decrypt_gost_csp(data: bytes, container_name: str) -> bytes: try: csp = _init_csp() if not csp.IsContainerExist(container_name): logging.error(f"Контейнер {container_name} не найден") raise RuntimeError(f"Контейнер {container_name} не существует") iv = data[:16] ciphertext = data[16:] decrypted = csp.DecryptData(container_name, ciphertext, iv, "GOST3412_2015_CTR") logging.info(f"Расшифровка через КриптоПро: контейнер={container_name}, размер={len(ciphertext)}") return decrypted except Exception as e: logging.error(f"Ошибка расшифровки ГОСТ: {e}") raise RuntimeError(f"Не удалось расшифровать данные через КриптоПро CSP: {e}") else: CSPTEST_PATH = "/opt/cprocsp/bin/amd64/csptest" def _check_csp_version(): try: cmd = [CSPTEST_PATH, "-info", "-version"] version = subprocess.check_output(cmd, timeout=5).decode() if not version.startswith("5."): raise RuntimeError("Требуется КриптоПро CSP 5.0+") logging.info(f"КриптоПро CSP версия: {version}") except Exception as e: logging.error(f"Ошибка проверки версии КриптоПро CSP: {e}") raise RuntimeError("Не удалось проверить версию КриптоПро CSP") def _check_container(container_name: str): cmd = [CSPTEST_PATH, "-keyset", "-container", container_name, "-verifycontext"] try: subprocess.check_call(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=5) except Exception: raise RuntimeError(f"Контейнер {container_name} не существует или недоступен") def _encrypt_gost_csp(data: bytes, container_name: str) -> bytes: _check_csp_version() _check_container(container_name) iv = secrets.token_bytes(16) cmd = [CSPTEST_PATH, "-keyset", "-container", container_name, "-encrypt", "-alg", "GOST3412_2015 /ctr", "-iv", iv.hex(), "-in", "-", "-out", "-"] try: p = subprocess.Popen(cmd, stdin=subprocess.PIPE, stdout=subprocess.PIPE, stderr=subprocess.PIPE) encrypted, err = p.communicate(input=data, timeout=10) if p.returncode != 0: logging.error(f"Ошибка шифрования ГОСТ: {err.decode()}") raise RuntimeError("Не удалось зашифровать данные через csptest") logging.info(f"Шифрование через КриптоПро: контейнер={container_name}, размер={len(data)}") return iv + encrypted except subprocess.TimeoutExpired: logging.error("Таймаут шифрования ГОСТ") raise RuntimeError("Таймаут при шифровании через csptest") def _decrypt_gost_csp(data: bytes, container_name: str) -> bytes: _check_csp_version() _check_container(container_name) iv = data[:16] ciphertext = data[16:] cmd = [CSPTEST_PATH, "-keyset", "-container", container_name, "-decrypt", "-alg", "GOST3412_2015 /ctr", "-iv", iv.hex(), "-in", "-", "-out", "-"] try: p = subprocess.Popen(cmd, stdin=subprocess.PIPE, stdout=subprocess.PIPE, stderr=subprocess.PIPE) decrypted, err = p.communicate(input=ciphertext, timeout=10) if p.returncode != 0: logging.error(f"Ошибка расшифровки ГОСТ: {err.decode()}") raise RuntimeError("Не удалось расшифровать данные через csptest") logging.info(f"Расшифровка через КриптоПро: контейнер={container_name}, размер={len(ciphertext)}") return decrypted except subprocess.TimeoutExpired: logging.error("Таймаут расшифровки ГОСТ") raise RuntimeError("Таймаут при расшифровке через csptest") # ==== Единый интерфейс ==== def encrypt_data(data: bytes) -> bytes: return _encrypt_gost_csp(data, GOST_CONTAINER) def decrypt_data(data: bytes) -> bytes: return _decrypt_gost_csp(data, GOST_CONTAINER) # ==== Основные функции ==== def get_from_cache(filename: str, src_dir: Optional[str] = None) -> str: ensure_cache_dir() load_access_counts() cpath = cache_path(filename) ipath = integrity_path(cpath) spath = os.path.join(src_dir or SOURCE_DIR, filename) access_counts[filename] += 1 save_access_counts() if os.path.exists(cpath) and not is_expired(cpath): valid = True raw_data = None if ENABLE_ENCRYPTION: try: with open(cpath, "rb") as f: encrypted = f.read() raw_data = decrypt_data(encrypted) except Exception as e: valid = False logging.warning(f"Ошибка расшифровки кэша: {filename}, {e}") else: try: with open(cpath, "rb") as f: raw_data = f.read() except Exception as e: valid = False logging.warning(f"Ошибка чтения кэша: {filename}, {e}") if valid and raw_data is not None: if os.path.exists(ipath): with open(ipath, "r") as f: stored_hash = f.read().strip() if hashlib.sha256(raw_data).hexdigest() == stored_hash: os.utime(cpath, None) logging.info(f"Файл загружен из кэша: {filename}") return cpath else: with open(ipath, "w") as f: f.write(hashlib.sha256(raw_data).hexdigest()) os.utime(cpath, None) logging.info(f"Создан хеш целостности: {filename}") return cpath for p in [cpath, ipath]: if os.path.exists(p): os.remove(p) logging.warning(f"Удалён повреждённый кэш: {p}") if not os.path.exists(spath): raise FileNotFoundError(f"Источник не найден: {spath}") with open(spath, "rb") as f: raw_data = f.read() with open(ipath, "w") as f: f.write(hashlib.sha256(raw_data).hexdigest()) if ENABLE_ENCRYPTION: encrypted = encrypt_data(raw_data) with open(cpath, "wb") as f: f.write(encrypted) else: with open(cpath, "wb") as f: f.write(raw_data) enforce_cache_limit() logging.info(f"Файл закэширован: {filename}") return cpath def fast_autosave(filename: str, content: bytes) -> str: ensure_cache_dir() cpath = cache_path(filename) ipath = integrity_path(cpath) with open(ipath, "w") as f: f.write(hashlib.sha256(content).hexdigest()) if ENABLE_ENCRYPTION: encrypted = encrypt_data(content) with open(cpath, "wb") as f: f.write(encrypted) else: with open(cpath, "wb") as f: f.write(content) os.utime(cpath, None) enforce_cache_limit() logging.info(f"Автосохранение: {filename}") return cpath if __name__ == "__main__": import argparse parser = argparse.ArgumentParser(description="HybridCache v2.3 (ФСТЭК-совместимая версия)") parser.add_argument("--mode", choices=["office", "media", "game"], default="office") parser.add_argument("--preload", help="Файлы для прогрева") parser.add_argument("--run-server", action="store_true") parser.add_argument("--file", help="Получить файл из кэша") parser.add_argument("--autosave-test", action="store_true") parser.add_argument("--gost-container", default=GOST_CONTAINER, help="Имя контейнера КриптоПро") args = parser.parse_args() GOST_CONTAINER = args.gost_container os.environ["HYBRIDCACHE_GOST_CONTAINER"] = GOST_CONTAINER
