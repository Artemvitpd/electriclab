""" HybridCache FULLSPEED SUITE v2.3 — ФСТЭК-совместимая версия - Поддержка КриптоПро CSP 5.0+ - Управление ключами через контейнеры - Логирование операций безопасности - Соответствие ФСТЭК №21, №38, №26 """ import os import sys import time import hashlib import json import logging import secrets from typing import List , Optional from collections import defaultdict from pathlib import Path import win32com.client # Для интеграции с КриптоПро CSP from cryptography.hazmat.primitives.ciphers import Cipher , algorithms , modes from cryptography.hazmat.backends import default_backend # === Настройка логирования безопасности === SECURITY_LOG = os.getenv ("HYBRIDCACHE_SECURITY_LOG", " hybridcache_security.log ") logging.basicConfig ( filename =SECURITY_LOG, level = logging.INFO , format ="%( asctime )s [SECURITY] %( message )s", encoding ="utf-8" ) # === Глобальные настройки === CACHE_DIR = os.getenv ("HYBRIDCACHE_DIR", "C:/HybridCache") SOURCE_DIR = os.getenv ("HYBRIDCACHE_SOURCE", "// Server / OfficeDocs ") MAX_CACHE_SIZE = int ( os.getenv ("HYBRIDCACHE_MAXSIZE", 100 * 1024**3)) CACHE_TTL = int ( os.getenv ("HYBRIDCACHE_TTL", 60 * 64 * 24 * 7)) ENABLE_ENCRYPTION = os.getenv ("HYBRIDCACHE_ENCRYPTION", "1") == "1" ENCRYPTION_MODE = os.getenv ("HYBRIDCACHE_ENC_MODE", " gost "). lower () # по умолчанию ГОСТ # === КриптоПро : имя контейнера (обязательно для ГОСТ) === GOST_CONTAINER = os.getenv ("HYBRIDCACHE_GOST_CONTAINER", " HybridCache_GOST ") ACCESS_COUNT_FILE = os.path.join (CACHE_DIR, ". access_count.json ") access_counts = defaultdict ( int ) def load_access_counts (): global access_counts if os.path.exists (ACCESS_COUNT_FILE): try : with open (ACCESS_COUNT_FILE, "r", encoding ="utf-8") as f: access_counts.update ( json.load (f)) except Exception : pass def save_access_counts (): try : with open (ACCESS_COUNT_FILE, "w", encoding ="utf-8") as f: json.dump ( dict ( access_counts ), f) except Exception : pass def integrity_path ( cpath : str ) -> str : return cpath + ".sha256" def ensure_cache_dir (): os.makedirs (CACHE_DIR, exist_ok = True ) def cache_path ( filename : str ) -> str : safe_name = filename.replace ("/", "_"). replace ("\\", "_"). replace ("..", "") return os.path.join (CACHE_DIR, safe_name ) def is_expired ( filepath : str ) -> bool : try : return ( time.time () - os.path.getmtime ( filepath )) > CACHE_TTL except Exception : return True def get_total_cache_size () -> int : total = 0 for f in os.listdir (CACHE_DIR): if f.endswith (".sha256") or f == ". access_count.json ": continue try : total += os.path.getsize ( cache_path (f)) except Exception : pass return total def enforce_cache_limit (): load_access_counts () files = [] for f in os.listdir (CACHE_DIR): if f.endswith (".sha256") or f == ". access_count.json ": continue cp = cache_path (f) try : mtime = os.path.getmtime ( cp ) size = os.path.getsize ( cp ) score = mtime + access_counts.get (f, 0) * 86400 files.append ((f, score , size )) except Exception : pass total = sum ( size for _, _, size in files ) if total <= MAX_CACHE_SIZE: return files.sort ( key = lambda x: x[1]) for f, _, size in files : if total <= MAX_CACHE_SIZE: break try : os.remove ( cache_path (f)) ipath = integrity_path ( cache_path (f)) if os.path.exists ( ipath ): os.remove ( ipath ) access_counts.pop (f, None ) total -= size except Exception : pass save_access_counts () # ==== ИНТЕГРАЦИЯ С КРИПТОПРО CSP ==== def _ init_csp () -> win32com.client.Dispatch: try : csp = win32com.client.Dispatch("CPCSP.CSP") logging.info ("Инициализация КриптоПро CSP успешна") return csp except Exception as e: logging.error ( f"Ошибка инициализации КриптоПро CSP: {e}") raise RuntimeError ("Не удалось инициализировать КриптоПро CSP") def _ encrypt_gost_csp ( data : bytes , container_name : str ) -> bytes : """ Шифрование через КриптоПро CSP 5.0+ (ГОСТ 34.12-2015, режим CTR) """ try : csp = _ init_csp () # Проверка существования контейнера if not csp.IsContainerExist ( container_name ): logging.error ( f"Контейнер { container_name } не найден") raise RuntimeError ( f"Контейнер { container_name } не существует") # Генерация IV (инициализационного вектора) iv = secrets.token_bytes (16) # Вызов ГОСТ 34.12-2015 в режиме CTR encrypted = csp.EncryptData ( container_name , data , iv, "GOST3412_2015_CTR") logging.info ( f"Шифрование через КриптоПро : контейнер={ container_name }, размер={ len ( data )}") return iv + encrypted except Exception as e: logging.error ( f"Ошибка шифрования ГОСТ: {e}") raise RuntimeError ( f"Не удалось зашифровать данные через КриптоПро CSP: {e}") def _ decrypt_gost_csp ( data : bytes , container_name : str ) -> bytes : """ Расшифровка через КриптоПро CSP """ try : csp = _ init_csp () # Проверка существования контейнера if not csp.IsContainerExist ( container_name ): logging.error ( f"Контейнер { container_name } не найден") raise RuntimeError ( f"Контейнер { container_name } не существует") iv = data [:16] ciphertext = data [16:] decrypted = csp.DecryptData ( container_name , ciphertext , iv, "GOST3412_2015_CTR") logging.info ( f"Расшифровка через КриптоПро : контейнер={ container_name }, размер={ len ( ciphertext )}") return decrypted except Exception as e: logging.error ( f"Ошибка расшифровки ГОСТ: {e}") raise RuntimeError ( f"Не удалось расшифровать данные через КриптоПро CSP: {e}") # ==== AES (для совместимости) ==== def _ encrypt_aes ( data : bytes ) -> bytes : key = secrets.token_bytes (32) # Безопасный ключ iv = secrets.token_bytes (16) # Безопасный IV cipher = Cipher ( algorithms.AES ( key ), modes.CTR (iv), backend = default_backend ()) encryptor = cipher.encryptor () return iv + encryptor.update ( data ) + encryptor.finalize () def _ decrypt_aes ( data : bytes ) -> bytes : iv = data [:16] ciphertext = data [16:] key = secrets.token_bytes (32) # Безопасный ключ cipher = Cipher ( algorithms.AES ( key ), modes.CTR (iv), backend = default_backend ()) decryptor = cipher.decryptor () return decryptor.update ( ciphertext ) + decryptor.finalize () # ==== Единый интерфейс ==== def encrypt_data ( data : bytes ) -> bytes : if ENCRYPTION_MODE == " gost ": return _ encrypt_gost_csp ( data , GOST_CONTAINER) else : return _ encrypt_aes ( data ) def decrypt_data ( data : bytes ) -> bytes : if ENCRYPTION_MODE == " gost ": return _ decrypt_gost_csp ( data , GOST_CONTAINER) else : return _ decrypt_aes ( data ) # ==== Основные функции (без изменений, кроме логирования ) ==== def get_from_cache ( filename : str , src_dir : Optional [ str ] = None ) -> str : ensure_cache_dir () load_access_counts () cpath = cache_path ( filename ) ipath = integrity_path ( cpath ) spath = os.path.join ( src_dir or SOURCE_DIR, filename ) access_counts [ filename ] += 1 save_access_counts () if os.path.exists ( cpath ) and not is_expired ( cpath ): valid = True raw_data = None if ENABLE_ENCRYPTION: try : with open ( cpath , " rb ") as f: encrypted = f.read () raw_data = decrypt_data ( encrypted ) except Exception as e: valid = False logging.warning ( f"Ошибка расшифровки кэша: { filename }, {e}") else : try : with open ( cpath , " rb ") as f: raw_data = f.read () except Exception as e: valid = False logging.warning ( f"Ошибка чтения кэша: { filename }, {e}") if valid and raw_data is not None : if os.path.exists ( ipath ): with open ( ipath , "r") as f: stored_hash = f.read (). strip () if hashlib.sha256( raw_data ). hexdigest () == stored_hash : os.utime ( cpath , None ) logging.info ( f"Файл загружен из кэша: { filename }") return cpath else : with open ( ipath , "w") as f: f.write (hashlib.sha256( raw_data ). hexdigest ()) os.utime ( cpath , None ) logging.info ( f"Создан хеш целостности: { filename }") return cpath # Удаление повреждённого кэша for p in [ cpath , ipath ]: if os.path.exists (p): os.remove (p) logging.warning ( f"Удалён повреждённый кэш: {p}") # Загрузка из источника if not os.path.exists ( spath ): raise FileNotFoundError ( f"Источник не найден: { spath }") with open ( spath , " rb ") as f: raw_data = f.read () with open ( ipath , "w") as f: f.write (hashlib.sha256( raw_data ). hexdigest ()) if ENABLE_ENCRYPTION: encrypted = encrypt_data ( raw_data ) with open ( cpath , " wb ") as f: f.write ( encrypted ) else : with open ( cpath , " wb ") as f: f.write ( raw_data ) enforce_cache_limit () logging.info ( f"Файл закэширован : { filename }") return cpath def fast_autosave ( filename : str , content : bytes ) -> str : ensure_cache_dir () cpath = cache_path ( filename ) ipath = integrity_path ( cpath ) with open ( ipath , "w") as f: f.write (hashlib.sha256( content ). hexdigest ()) if ENABLE_ENCRYPTION: encrypted = encrypt_data ( content ) with open ( cpath , " wb ") as f: f.write ( encrypted ) else : with open ( cpath , " wb ") as f: f.write ( content ) os.utime ( cpath , None ) enforce_cache_limit () logging.info ( f"Автосохранение : { filename }") return cpath # … (остальные функции: preload , режимы, API — без изменений) # CLI и основной блок — без изменений if __ name __ == "__ main __": import argparse parser = argparse.ArgumentParser ( description =" HybridCache v2.3 (ФСТЭК-совместимая версия)") parser.add_argument ("-- mode ", choices =[" office ", " media ", " game "], default =" office ") parser.add_argument ("-- preload ", help ="Файлы для прогрева") parser.add_argument ("-- run-server ", action =" store_true ") parser.add_argument ("-- file ", help ="Получить файл из кэша") parser.add_argument ("-- autosave-test ", action =" store_true ") parser.add_argument ("-- enc-mode ", choices =[" aes ", " gost "], default =ENCRYPTION_MODE) parser.add_argument ("-- gost-container ", default =GOST_CONTAINER, help ="Имя контейнера КриптоПро ") args = parser.parse_args () ENCRYPTION_MODE = args.enc_mode GOST_CONTAINER = args.gost_container os.environ ["HYBRIDCACHE_ENC_MODE"] = ENCRYPTION_MODE os.environ ["HYBRIDCACHE_GOST_CONTAINER"] = GOST_CONTAINER # … (остальной CLI-код без изменений)
